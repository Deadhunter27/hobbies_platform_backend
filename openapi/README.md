# OpenAPI artifacts

`openapi.json` is generated here from the NestJS code (see docs/guides/openapi.md) starting in Phase 2. The committed spec is the single source of truth for the API contract; the mobile repository generates its TypeScript types from it (ADR-0005).
